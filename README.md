# ConnectX

Workspace scaffold for a future .NET application.

## Structure

- `src/` - Future .NET solution and application projects.
- `tests/` - Future automated test projects.
- `docs/` - Project notes and supporting documentation.

